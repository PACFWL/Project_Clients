<h1>Listagem - Agendamento de Potenciais Clientes</h1>
<p>Sistema Utilizado para agendamento de serviços.</p>
<a href="/customers/add" class="btn btn-primary">Adicionar cliente</a>
<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Telefone</th>
      <th>Origem</th>
      <th>Data do Contacto</th>
      <th>Observação</th>
      <th>Ações</th>
    </tr>
  </thead>
  <tbody>
    {{#each customers}}
    <tr>
      <td>{{this.nome}}</td>
      <td>{{this.telefone}}</td>
      <td>{{this.origem}}</td>
      <td>{{formatDateSimplified this.dataDoContacto}}</td>
      <td>{{this.observacao}}</td>
     <td>
  <div class="btn-group" role="group" aria-label="Basic example">
    <a href="/customers/view/{{this.id}}" class="btn btn-info mr-2">Visualizar</a>
    <a href="/customers/edit/{{this.id}}" class="btn btn-warning mr-2">Editar</a>
    <a href="/customers/delete/{{this.id}}" class="btn btn-danger mr-2">Eliminar</a>
  </div>
</td>
    </tr>
    {{/each}}
  </tbody>
</table>
